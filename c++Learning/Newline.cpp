// Code to inser a new line using \n 
#include <iostream> 

int main(){
    std::cout<< "hello guys now we did not use the namespace\n"; // forcing the cursor to go to next line 
    std::cout<< " Cause we did not specify to use the name space std initially , we have to add the std keyword infront of the code";
    return 0;
}